# API Contracts

**This is a local desktop application and does not expose any external REST or GraphQL APIs.**

The application interacts directly with a local SQLite database for data storage and retrieval. All business logic is contained within the Python application code.
